[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers -p no:launch_testing_ros_pytest_entrypoint
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
